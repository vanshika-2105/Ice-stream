# ICE-STREAM Data Quality

## Purpose

The ICE-STREAM data-quality layer validates checkout events before
they are processed by downstream components.

The validation logic is implemented in:

```text
quality-rules/validator.py